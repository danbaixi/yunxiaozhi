<view class="container" wx:if="{{display}}">
  <view class="notice" style="{{ Style }}">
    <view class="content" bindtap="go">{{content}}</view>
    <text class="icon-close" bindtap="close"></text>
  </view>
</view>