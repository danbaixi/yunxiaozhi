<!-- <view class="diy-nav" style="height:{{CustomBar}}px;">
  <view class='select_week' style="height:{{CustomBar}}px;padding-top:{{StatusBar}}px;">
    <picker class='week_num' mode="selector" range="{{zhou_num}}" bindchange="select" value="{{now_week-1}}">
      <text class="text-white text-lg">第 {{now_week}} 周 </text>
      <text class="icon-unfold lg text-white"></text>
    </picker>
    <view class="text-white text-lg settings" style="height:{{CustomBar}}px;padding-top:{{StatusBar}}px;" bindtap='listDisplay'>
    更新
    </view> 
  </view>
</view> -->
<view class="diy-nav">
  <view class='select_week'>
    <picker class='week_num' mode="selector" range="{{zhou_num}}" bindchange="select" value="{{now_week-1}}">
      <text class="text-white text-lg">第 {{now_week}} 周 </text>
      <text class="icon-unfold lg text-white"></text>
    </picker>
    <text class="text-white text-lg settings" bindtap='listDisplay'>设置</text> 
  </view>
</view>
<view class="mask" bindtap="hideMask" style='height:{{winHeight-32}}px' wx:if="{{list_is_display}}"></view>
<view class='list' style='top:{{list_is_display?32:-180}}px'>
  <view bindtap='update' class='li'>
    更新课表
    <text class="right icon-right text-theme text-lg"></text>
  </view>
  <view bindtap='selectImg' class='li'>
    <text class='text_left'>课表背景</text>
    <text class="right icon-right text-theme text-lg"></text>
    <text class='text_right'>{{imageUrl!=''?'已设置':'未设置'}}</text>
  </view>
  <view class='li'>
    <text class='text_left'>边框透明度</text>
    <view class="slider">
      <slider data-type='frime' bindchange="sliderchange" activeColor="#6ed4e6" value='{{Kopacity}}' min="0" max="100" show-value/>
    </view>
  </view>
  <view class='li'>
    <text class='text_left'>课表透明度</text>
    <view class="slider">
      <slider data-type='course' bindchange="sliderchange" activeColor="#6ed4e6" value='{{Copacity}}' min="0" max="100" show-value/>
    </view>
  </view>
  <view class='li'>
    <text class='text_left'>字体大小</text>
    <view class="slider">
      <slider data-type='font' bindchange="sliderchange" activeColor="#6ed4e6" value='{{fontSize}}' min="6" max="12" show-value/>
    </view>
  </view>

</view>

<!-- 背景图片 -->
<view class='content'>
  <image class="course_bg" src="{{imageUrl}}" mode="aspectFill"> </image>
</view>

<view class="top" style="background-color:rgba(220,220,220,{{(100-Kopacity)/100}})">
    <view class='jie month'>
      <view>{{now_month}}</view>
      <view>月</view>
    </view>
    <view class="date">
      <view class="course-table week {{(todayMonth==now_month&&todayDay==item)?'todayDate':''}}" wx:for="{{now_day}}" wx:key="key" wx:index="index">
        <view class="week-item">周{{zhou[index]}}</view>
        <view class="date-item">{{now_day[index] == 1? now_month+1+'月' : now_day[index]+'日'}}</view>
      </view>
    </view>
</view>

  <scroll-view scroll-y="true" class="scroll">
    <view class='course-content'>
      <view class='jie' style="background-color:rgba(220,220,220,{{(100-Kopacity)/100}})">
        <view wx:for="{{jie}}" wx:key="key" class="left">
          {{item}}
        </view>
      </view>

      <view class="course">
        <view class="{{toggleDelay?'animation-scale-up':''}} kcb-item" wx:for="{{course}}" wx:key="key" bindtap="displayCourseInfo" data-num="{{item.indexNum}}" form-type="submit" style="animation-delay: {{(index%2==0?(index/2):(index-1)/2+1)*0.1}}s;margin-left:{{(item.week-1)*(92/7)}}%;margin-top:{{(item.jie-1)*110+4}}rpx;height:{{item.jieshu*110 - 4}}rpx">
        <view class="smalltext" style="background-color:rgba({{colorArrays[item.num]}},{{(100-Copacity)/100}});" >{{item.name}}@{{item.address}}</view>
        </view>

        <view wx:if="{{ad.display}}" bindtap="goAd" class='kcb-item' style="margin-left:{{(ad.week-1)*(92/7)}}%;margin-top:{{(ad.jie-1)*110+4}}rpx;height:{{ad.jieshu*110 - 4}}rpx">
          <view class="smalltext" style="background:{{ad.background}};color:{{ad.color}};" >{{ad.title}}</view>
          <view bindtap="closeAd" class="ad-close icon-roundclose text-center text-grey"></view>
        </view>

        <view class='train' data-id="{{train_course_id}}" bindtap='train' wx:if="{{train_course_id!=0}}">本周是实训周</view>

      </view>
    </view>
  </scroll-view>
  <!--更新课表输入验证码弹窗-->
  <view class="modal-mask" bindtap="hideModal" style='height:{{winHeight}}px;' catchtouchmove="preventTouchMove" wx:if="{{showModal}}"></view>
  <view class="modal-dialog" wx:if="{{showModal}}" class="{{input_focus == 1?'modal-dialog input_focus':'modal-dialog'}}">
    <view class="modal-title">更新课表</view>
      <view class="modal-content">
        <view class="modal-input">
          <input placeholder-class="input-holder" bindfocus="inputFocus" bindblur="inputBlur" bindinput="yzmInput" maxlength="4" class="input">
          </input>
          <image bindtap='freshYzm' class="img_yzm" src="{{yzmUrl}}"></image>
        </view>  
      </view>
      <view class="modal-footer">
        <view class="btn-cancel" bindtap="onCancel" data-status="cancel">取消</view>
        <view class="btn-confirm" bindtap="update" data-status="confirm">确定</view>
      </view>
</view>